import React from 'react';
import {StyleSheet, Text, View} from 'react-native';
import tw from '../../../lib/tailwind';

const Tickets = () => {
  return (
    <View>
      <Text style={tw`text-primary`}>Tickets</Text>
    </View>
  );
};

export default Tickets;

const styles = StyleSheet.create({});
